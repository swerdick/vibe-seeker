services:
  postgres:
    image: postgres:18-bookworm
    environment:
      POSTGRES_USER: vibe_seeker
      POSTGRES_PASSWORD: vibe_seeker
      POSTGRES_DB: vibe_seeker
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
